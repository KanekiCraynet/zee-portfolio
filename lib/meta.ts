// Product metadata for runtime — NOT imported from package.json
// because OpenNext/Cloudflare edge bundler resolves JSON modules
// inconsistently. Keep in sync with package.json manually.

export const APP_NAME = "zee-portfolio";
export const APP_VERSION = "0.1.0";
